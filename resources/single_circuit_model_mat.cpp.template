// This file has been automatically generated by the pyALS tool.
// Please, check it out at https://github.com/SalvatoreBarone/pyALS

#include "{{items['cc_header']}}"

const {{items["res_c_type"]}} {{items["top_module"]}}::model [{{items["op1_c_size"]}}][{{items["op2_c_size"]}}] = {
{% for row in items["lut"] -%}{{"\t{"}}{% for col in row -%}{{col}}{{ ", " if not loop.last else "" }}{% endfor %}{{ "}, " if not loop.last else "}" }}
{% endfor %}};